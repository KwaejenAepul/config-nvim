My smacked together nvim config

This is mostly thrown together and would not recommend you use this for your own

godot stuff:
--server ./godothost --remote-send "<C-\><C-N>:e {file}<CR>:call cursor({line}+1,{col})<CR>"
